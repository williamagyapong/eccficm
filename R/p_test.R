
# A generic function for performing the permutation test
p.test <- function(test_stats) {
  # reps <- replicate(B,eccfic(x,y[sample(1:n),],est,ns,kernel,bw,sigma,index, wt)[[tstats.index]])
  # pvalue <- (sum(reps>=test.statistic)+1)/(B+1)
  # return(pvalue)
}
